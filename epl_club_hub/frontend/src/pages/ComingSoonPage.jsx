function ComingSoonPage({ title }) {
  return (
    <main className="section">
      <div className="coming-soon-card-react">
        <h1>{title}</h1>
        <p>이 페이지는 다음 단계에서 만들 거야.</p>
      </div>
    </main>
  );
}

export default ComingSoonPage;